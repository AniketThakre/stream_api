import java.util.Arrays;
import java.util.List;
import java.util.stream.Collectors;

public class Filtering {
    public static void main(String[] args) {

//        1. Filter a list of integers to include only numbers greater than 10 using streams.
//        List<Integer> list = Arrays.asList(1,4,6,3,19,34,2,54,2);
//        List<Integer> res = list.stream().filter(n -> n>10).collect(Collectors.toList());
//        System.out.println(res);

//        2. Filter a list of strings to include only those that start with a vowel using streams.
//        List<String> list = Arrays.asList("Aniket","Sumita","Evil","Oliv","Burger");
//        List<String> res = list.stream()
//                        .filter(n-> n.startsWith("A") ||
//                                n.startsWith("E") ||
//                                n.startsWith("I") ||
//                                n.startsWith("O") ||
//                                n.startsWith("U") ||
//                                n.startsWith("a") ||
//                                n.startsWith("e") ||
//                                n.startsWith("i") ||
//                                n.startsWith("o") ||
//                                n.startsWith("u"))
//                        .collect(Collectors.toList());
//        System.out.println(res);

//        3. Filter a list of objects by a specific property using streams.
//        List<Student> list = Arrays.asList(
//                new Student(101,"Aniket",80),
//                new Student(102,"Anmol",90),
//                new Student(103,"Sumita",95)
//        );
//        List<Student> res = list.stream().filter(s -> s.getScore()>=90).collect(Collectors.toList());
//        System.out.println(res);

//        4. Remove all null elements from a list of strings using streams.
//        List<Student> list = Arrays.asList(
//                new Student(101,"Aniket",80),
//                null,
//                new Student(102,"Anmol",90),
//                new Student(103,"Sumita",95),
//                null
//        );
//        List<Student> res = list.stream().filter(s -> s!=null).collect(Collectors.toList());
//        System.out.println(res);

//        5. Filter a list of people to include only adults (age 18+) using streams.
//        List<People> list = Arrays.asList(
//                new People(1,"Aniket",27),
//                new People(2,"Anmol",29),
//                new People(3,"Sumita",59),
//                new People(4,"Tunnu",5)
//        );
//        List<People> res = list.stream().filter(s -> s.getAge()>18).collect(Collectors.toList());
//        System.out.println(res);


    }
}
